% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refit_poll_of_polls.R
\name{assert_warm_start_is_compatible}
\alias{assert_warm_start_is_compatible}
\title{Assert warm-start compatibility before sampling}
\usage{
assert_warm_start_is_compatible(x, constructor_args, sample_args)
}
\arguments{
\item{x}{Existing [poll_of_polls] object that provides the stored fit used
for warm-start defaults.}

\item{constructor_args}{Named constructor argument list for the refit call.}

\item{sample_args}{Named sampler argument list after warm-start arguments
have been materialized.}
}
\value{
Invisible `TRUE` when the selected warm-start settings are
  compatible with the refit. Otherwise an error is thrown before sampling.
}
\description{
Validate that the warm-start inputs selected for a refit are compatible with
the requested chain count and with the parameter dimensions implied by the
refit model. When `polls_data` changes, this helper rebuilds the refit model
dimensions before sampling so incompatible `init` values or inverse metrics
fail early with a focused error.
}
\keyword{internal}
