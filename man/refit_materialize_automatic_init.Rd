% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refit_poll_of_polls.R
\name{refit_materialize_automatic_init}
\alias{refit_materialize_automatic_init}
\title{Materialize automatic init reuse for a refit}
\usage{
refit_materialize_automatic_init(x, chains, init_mode, sample_args)
}
\arguments{
\item{x}{Existing [poll_of_polls] object being refit.}

\item{chains}{Integer scalar giving the refit chain count.}

\item{init_mode}{Automatic init mode, either `"last"` or `"random"`.}

\item{sample_args}{Named sampler argument list for the refit call.}
}
\value{
A named list with elements `init` and `source_chain_ids`.
}
\description{
Build the automatic `init` payload used by [refit_poll_of_polls()] when the
caller does not provide an explicit `warm_start$init`. In `"last"` mode the
helper reuses the final draw from each stored chain and requires the refit
chain count to match the stored fit's chain count (`fitted_chains`). In
`"random"` mode it samples one post-warmup posterior draw per refit chain
and records which source chain each sampled draw came from.
}
\keyword{internal}
